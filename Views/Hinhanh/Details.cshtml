@model DoAn3.Models.Hinhanh

@{
    ViewData["Title"] = "Details";
}

<section id="main-content">
  <section class="wrapper">
    <h3>Chi tiết hình ảnh</h3>
    <div class="row">
      <div class="col-md-12">
        <div class="content-panel">
          <table class="table table-striped table-advance table-hover">
            <thead>
              <tr>
                <th>Mã hình ảnh</th>
                <th>Tên hình ảnh</th>
                <th>Hình ảnh</th>
                <th></th>
              </tr>
                <tr>
                    <td>
                        @Html.DisplayFor(model=>model.HaMa)
                    </td>
                    <td>
                        @Html.DisplayFor(model=>model.HaTen)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.HaUrl)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@Model.HaMa"class="btn btn-primary"><i class="fa fa-pencil"></i></a> |
                        <a asp-action="Index" class="btn btn-default "><i class="fa fa-rotate-left"></i></a>
                    </td>
                </tr>
            </thead>
          </table>
        </div>
      </div>
    </div>
  </section>
</section>